# Copy to dspm-bridge.env and edit. Read by the systemd unit on the Pi.
# norns' IP on the Opal LAN (give norns a static DHCP lease so this never moves):
NORNS_HOST=192.168.8.20
NORNS_PORT=10111
BRIDGE_WS_PORT=8081
# BRIDGE_TICK_MS=40
# SESSIONS_DIR=/home/pi/dspm-archive/sessions
